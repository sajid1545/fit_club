import React from 'react';

const Card = ({ exercise }) => {
    
    const { title,picture,time,age } = exercise;

    return (
        <div>
            
        </div>
    );
};

export default Card;